apiVersion: monitoring.coreos.com/v1alpha1
kind: AlertmanagerConfig
metadata:
  name: forum-telegram
  namespace: jon
  labels:
    release: monitoring
    app.kubernetes.io/part-of: kube-prometheus-stack
spec:
  route:
    receiver: forum-telegram
    groupBy:
      - alertname
      - pod
    groupWait: 30s
    groupInterval: 5m
    repeatInterval: 12h
    matchers:
      - name: app
        matchType: "="
        value: forum
  receivers:
    - name: forum-telegram
      telegramConfigs:
        - botToken:
            name: forum-telegram-alerts
            key: bot_token
          chatID: -1003003060205
          parseMode: HTML
          sendResolved: true
